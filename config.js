// Power Zone E-Commerce Website Configuration
window.CONFIG = {
  shopName: "Power Zone",
  whatsappNumber: "94774853381", // Format: country code + number (no spaces, no +)
  adminPassword: "admin123",     // Default password for Admin Panel (change this!)
  
  // Supabase Configuration (Optional - for real-time cloud database)
  // To enable, sign up at supabase.com, create a project, and fill in these keys.
  // If left empty, the site will use localStorage and products.json.
  supabaseUrl: "",
  supabaseKey: ""
};
